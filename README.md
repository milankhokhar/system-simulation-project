# system-simulation-project
Instead of putting notes in slides, I put comments for every snippet. So, it is easy to understand the code.
I also attached the data file(income_data.txt).
For getting output, you have to put code and data file in the same folder. 
Also, you have to install needed packages.
Program takes time to train the classifier. Because of 25000 datapoints is used for each class to train the classifier.
I took a large number of data points to train the classifier. So that, I can predict the result more accurately.
